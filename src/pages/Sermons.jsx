import SectionTitle from "../components/common/SectionTitle";


function Sermons () { 
    return (
        <div>
            <SectionTitle
            title="Sermons Page"
            />
        </div>
    )
}

export default Sermons;